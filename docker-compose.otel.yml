# OTel overlay — rebuilds with OTel support + adds Jaeger for trace visualization.
#
# Usage:
#   docker compose -f docker-compose.yml -f docker-compose.postgres.yml -f docker-compose.otel.yml up
#
# Jaeger UI: http://localhost:16686

services:
  jaeger:
    image: jaegertracing/all-in-one:1.68.0
    ports:
      - "16686:16686"   # Jaeger UI
      - "4317:4317"     # OTLP gRPC
      - "4318:4318"     # OTLP HTTP
    environment:
      - COLLECTOR_OTLP_ENABLED=true
    restart: unless-stopped

  vbpclaw:
    build:
      args:
        ENABLE_OTEL: "true"
    environment:
      - VBPCLAW_TELEMETRY_ENABLED=true
      - VBPCLAW_TELEMETRY_ENDPOINT=jaeger:4317
      - VBPCLAW_TELEMETRY_PROTOCOL=grpc
      - VBPCLAW_TELEMETRY_INSECURE=true
      - VBPCLAW_TELEMETRY_SERVICE_NAME=vbpclaw-gateway
    depends_on:
      jaeger:
        condition: service_started
