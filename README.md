# Memory Index

- [Rebuild workflow] — Dùng `./start.sh up vbpclaw` (không phải restart) sau khi build để apply image mới
- [Git push requires approval](feedback_git_push.md) — Không tự push git, chờ user báo mới push
